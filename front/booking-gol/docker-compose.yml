version: '3.8'

services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gdadc-ui
    ports:
      - "4200:80"
    restart: unless-stopped
    networks:
      - gdadc-network

networks:
  gdadc-network:
    driver: bridge
